// ============================================================================
// Nexus Agent Platform — Config Barrel Export
// ============================================================================

export { runtimeConfig, default } from "./runtime.config";
export type { RuntimeConfig } from "@/engine/types/config-types";
